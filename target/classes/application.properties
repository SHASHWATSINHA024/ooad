    # MySQL Configuration
    spring.datasource.url=jdbc:mysql://localhost:3306/librarydb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    spring.datasource.username=root
    spring.datasource.password=rock123
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

    # Hibernate DDL Auto (update will create the tables automatically)
    spring.jpa.hibernate.ddl-auto=update

    # Show SQL queries in the console (optional)
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.format_sql=true

    # JPA Database dialect
    spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
